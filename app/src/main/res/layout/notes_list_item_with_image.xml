<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/main_margin"
    android:layout_marginVertical="@dimen/short_margin"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tv_note_item_title"
        style="@style/title_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:lines="1"
        android:textAlignment="center"
        app:layout_constraintBottom_toTopOf="@id/tv_note_item_content"
        app:layout_constraintEnd_toStartOf="@id/iv_note_item_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Title" />

    <TextView
        android:id="@+id/tv_note_item_content"
        style="@style/content_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginVertical="@dimen/short_margin"
        android:lines="1"
        app:layout_constraintBottom_toTopOf="@id/tv_note_item_date"
        app:layout_constraintEnd_toStartOf="@id/iv_note_item_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_note_item_title"
        tools:text="Content" />

    <TextView
        android:id="@+id/tv_note_item_date"
        style="@style/date_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:lines="1"
        android:textAlignment="textEnd"
        app:layout_constraintBottom_toTopOf="@id/divider"
        app:layout_constraintTop_toBottomOf="@id/iv_note_item_image"
        tools:text="Date" />

    <ImageView
        android:id="@+id/iv_note_item_image"
        android:layout_width="@dimen/image_size"
        android:layout_height="@dimen/image_size"
        android:layout_marginVertical="@dimen/short_margin"
        app:layout_constraintBottom_toBottomOf="@id/tv_note_item_date"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/tv_note_item_content"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?android:attr/listDivider"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_note_item_date" />
</androidx.constraintlayout.widget.ConstraintLayout>